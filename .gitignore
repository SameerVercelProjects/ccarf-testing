# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# env / secrets
.env
.env*.local

# plaintext credentials — never commit
# (config/users.json holds only bcrypt hashes and IS committed so the deployed
#  app can read it without relying on the USERS_JSON env var)
/config/users_plain.json

# os
.DS_Store
*.log
