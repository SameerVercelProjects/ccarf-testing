# Secret used to sign session cookies (jose HS256).
# Generate a strong value, e.g.:
#   openssl rand -base64 48
# The app fails to start / validate sessions if this is not set.
SESSION_SECRET=replace-with-a-long-random-secret
